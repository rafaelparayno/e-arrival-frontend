import React from "react";
import MultiForm from "../../components/NewInput/MultiForm";

const AddData = (props) => {
  return (
    <main>
      <MultiForm />
    </main>
  );
};

export default AddData;
